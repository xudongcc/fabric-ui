---
title: Page
description: A layout component for building consistent page structures with title, description, and actions.
---

<Preview path="page" />

## Installation

```bash
pnpm dlx shadcn@latest add @fabric-ui/page
```

## Usage

```tsx
import { Page } from "@/components/fabric-ui/page";
```

```tsx
<Page title="Products" description="Manage your products and inventory.">
  {/* Page content */}
</Page>
```

## Examples

### With Back Action

Use the `backAction` prop to add a back button to the page header.

<Preview path="page-with-back-action" />

### With Actions

Use `primaryAction` and `secondaryActions` props to add action buttons to the page header.

<Preview path="page-with-actions" />

### Full Width

Use the `variant="full"` prop to remove the default max-width constraint.

<Preview path="page-full-width" />

## API Reference

### Page

The root container component.

<AutoTypeTable
  path="../../components/page/index.tsx"
  type="Pick<PageProps, 'title' | 'description' | 'backAction' | 'primaryAction' | 'secondaryActions' | 'variant' | 'className'>"
/>

### PageBackActionProps

The back action props for the page header.

<AutoTypeTable
  path="../../components/page/index.tsx"
  type="PageBackActionProps"
/>

### PageActionProps

The action props for `primaryAction` and `secondaryActions`.

<AutoTypeTable path="../../components/page/index.tsx" type="PageActionProps" />
